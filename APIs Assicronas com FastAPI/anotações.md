add com poetry
    "fastapi[standard]"
    
    poetry add 'uvicorn[standart]'

para verificar se ta td certo
    python+ espaço 

    dir(fastapi)

    fastapi.__version__

uvicorn src.main:app --reload (salvar e run)


n fz
    conexão banco de dados específicos
